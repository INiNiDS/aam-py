version: 2

build:
  os: ubuntu-24.04 
  tools:
    python: "3.14" 

sphinx:
  configuration: null
  
commands:
  - curl -LsSf https://astral.sh/uv/install.sh | sh
  - $HOME/.cargo/bin/uv sync --all-extras --dev
  - $HOME/.cargo/bin/uv run zensical build --site-dir $READTHEDOCS_OUTPUT/html
